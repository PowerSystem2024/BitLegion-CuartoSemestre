# Gestor de Tareas
Proyecto creado para arrendar tareas

Tecnologías principales
- Frontend: React + Vite
- Backend: Node + Express
- Base de datos: PostgreSQL

Instalación 
# dependencias del backend
cd backend || cd server || cd api
npm install

# dependencias del frontend + run
cd ../frontend || cd client
npm install
npm run dev
